# Paquetes de primer nivel, pineados desde reportes/pre-venv-freeze.txt (venv armado 24/09 12:55 AR,
# Python 3.13.15). Las dependencias transitivas las resuelve pip; ya probaron instalar juntas una vez
# (ese freeze es la evidencia). Si un agente instala algo nuevo, lo declara en su reporte y se agrega acá.
google-genai==2.25.0
websockets==16.1.1
aiohttp==3.14.3
numpy==2.5.3
yt-dlp==2026.8.19
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.3
jsonschema==4.26.0
